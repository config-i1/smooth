name: Python Tests

on:
  pull_request:
    paths:
      - 'python/**'
      - '.github/workflows/python_ci.yml'
  workflow_dispatch:

# Opt every JavaScript action (including those bundled inside
# ``pypa/cibuildwheel@v3.1``, e.g. ``actions/setup-python@v5``) into
# the Node.js 24 runtime. GitHub deprecates Node.js 20 on 2026-06-02
# and removes it on 2026-09-16; this env var is the documented opt-in
# mechanism for the transition period.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install ruff and mypy
        run: pip install ruff mypy

      - name: Run ruff linter
        run: ruff check python/src/smooth/

      - name: Run ruff formatter check
        run: ruff format --check python/src/smooth/

      - name: Run mypy
        working-directory: python
        run: mypy src/smooth

  build-and-test:
    name: Build & Test (${{ matrix.os }}, Python ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux
          - os: ubuntu-latest
            python-version: "3.11"
            cibw-build: "cp311-manylinux*"
          - os: ubuntu-latest
            python-version: "3.12"
            cibw-build: "cp312-manylinux*"
          - os: ubuntu-latest
            python-version: "3.13"
            cibw-build: "cp313-manylinux*"
          - os: ubuntu-latest
            python-version: "3.14"
            cibw-build: "cp314-manylinux*"
          # macOS
          - os: macos-latest
            python-version: "3.11"
            cibw-build: "cp311-macosx*"
          # Windows — pin to windows-2025 (newer Server 2025 host, but
          # still a stable VS17 toolchain on GitHub's image) rather than
          # ``windows-latest``. ``windows-latest`` now rolls to a VS18 /
          # MSVC 14.51.36231 preview toolchain that openblas 0.3.33 (the
          # vcpkg port we pull in CIBW_BEFORE_ALL_WINDOWS) doesn't
          # compile under, killing the ``vcpkg install
          # openblas:x64-windows-release`` step.
          - os: windows-2025
            python-version: "3.11"
            cibw-build: "cp311-win_amd64"

    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Build and test with cibuildwheel
        uses: pypa/cibuildwheel@v3.1
        with:
          package-dir: python
          output-dir: wheelhouse
        env:
          CIBW_BUILD: ${{ matrix.cibw-build }}
          CIBW_ARCHS_LINUX: x86_64
          CIBW_ARCHS_MACOS: x86_64 arm64
          CIBW_ARCHS_WINDOWS: AMD64
          CIBW_BEFORE_ALL_LINUX: |
            yum install -y openblas-devel lapack-devel pkgconfig || \
            (apt-get update && apt-get install -y libopenblas-dev liblapack-dev pkg-config)
          CIBW_ENVIRONMENT_LINUX: PKG_CONFIG_PATH="/usr/lib64/pkgconfig:/usr/lib/pkgconfig"
          CIBW_BEFORE_ALL_MACOS: brew install openblas
          # Pin vcpkg to a release before vcpkg-gfortran@3#3 switched
          # from MinGW gfortran to LLVMFlang. LLVMFlang 22.x cannot
          # build lapack-reference 3.12.1 (the install step exits with
          # BUILD_FAILED inside the Fortran compile), which kills the
          # whole CIBW_BEFORE_ALL_WINDOWS step. The 2025.06.13 tag is
          # the latest known-good baseline; if a later regression
          # surfaces, fall back to 2025.04.09 / 2025.01.13.
          CIBW_BEFORE_ALL_WINDOWS: >
            git -C C:/vcpkg fetch --tags &&
            git -C C:/vcpkg checkout 2025.06.13 &&
            C:/vcpkg/bootstrap-vcpkg.bat -disableMetrics &&
            vcpkg install openblas:x64-windows-release lapack:x64-windows-release
          CIBW_BEFORE_BUILD_WINDOWS: pip install delvewheel
          CIBW_ENVIRONMENT_WINDOWS: CMAKE_ARGS="-DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake -DVCPKG_TARGET_TRIPLET=x64-windows-release"
          CIBW_REPAIR_WHEEL_COMMAND_WINDOWS: delvewheel repair -w {dest_dir} {wheel} --add-path C:/vcpkg/installed/x64-windows-release/bin
          CIBW_TEST_REQUIRES: pytest numpy
          CIBW_TEST_COMMAND: pytest {project}/python/tests -v --tb=short
