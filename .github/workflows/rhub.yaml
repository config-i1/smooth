# R-hub workflow. Adapted from r-hub/actions/blob/v1/workflows/rhub.yaml.
#
# Changes from the canonical template:
#   * The default platform list is the modern CRAN-submission matrix
#     (linux, windows, macos-arm64) instead of the upstream
#     "linux,windows,macos-latest".  ``macos-latest`` is a GitHub-runner
#     label, not an R-hub platform — the upstream default errored at the
#     setup step with "no such platform: macos-latest".
#   * ``macos`` (Intel) is deliberately NOT in the default list. R-hub
#     maps the platform name ``macos`` → runner label ``macos-13``,
#     which is the last Intel macOS image; GitHub has effectively
#     retired ``macos-13``'s runner inventory and jobs targeting it
#     queue forever with "Waiting for a runner to pick up this job…".
#     The CRAN macOS farm runs on Apple Silicon, so ``macos-arm64`` is
#     the right modern target. Add ``macos`` back manually via the
#     ``config`` input only if GitHub restores Intel runner capacity.
#
# The ``config`` workflow_dispatch input overrides the default platform
# list. Any comma-separated subset of:
#   linux, macos, macos-arm64, windows,
#   atlas, c23, clang-asan, clang-ubsan, clang16-22, donttest,
#   gcc-asan, gcc13-16, intel, lto, m1-san, mkl, nold, noremap,
#   nosuggests, rchk, ubuntu-clang, ubuntu-gcc12, ubuntu-next,
#   ubuntu-release, valgrind, vnu
# (see ``rhub::rhub_platforms()$name`` for the current list).

name: R-hub
run-name: "${{ github.event.inputs.id }}: ${{ github.event.inputs.name || format('Manually run by {0}', github.triggering_actor) }}"

on:
  workflow_dispatch:
    inputs:
      config:
        description: 'Comma-separated list of R-hub platforms. Leave empty to use the default CRAN-submission matrix.'
        type: string
        default: 'linux,windows,macos-arm64'
      name:
        description: 'Run name. You can leave this empty.'
        type: string
      id:
        description: 'Unique ID. You can leave this empty.'
        type: string

jobs:

  setup:
    runs-on: ubuntu-latest
    outputs:
      containers: ${{ steps.rhub-setup.outputs.containers }}
      platforms: ${{ steps.rhub-setup.outputs.platforms }}

    steps:
    # NO NEED TO CHECKOUT HERE.
    # ``github.event.inputs.config`` is only populated on workflow_dispatch;
    # fall back to the default modern CRAN-submission matrix otherwise.
    # See header comment for why ``macos`` (Intel) is not in the default.
    - uses: r-hub/actions/setup@v1
      with:
        config: ${{ github.event.inputs.config || 'linux,windows,macos-arm64' }}
      id: rhub-setup

  linux-containers:
    needs: setup
    if: ${{ needs.setup.outputs.containers != '[]' }}
    runs-on: ubuntu-latest
    name: ${{ matrix.config.label }}
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJson(needs.setup.outputs.containers) }}
    container:
      image: ${{ matrix.config.container }}

    steps:
      - uses: r-hub/actions/checkout@v1
      - uses: r-hub/actions/platform-info@v1
        with:
          token: ${{ secrets.RHUB_TOKEN }}
          job-config: ${{ matrix.config.job-config }}
      - uses: r-hub/actions/setup-deps@v1
        with:
          token: ${{ secrets.RHUB_TOKEN }}
          job-config: ${{ matrix.config.job-config }}
      - uses: r-hub/actions/run-check@v1
        with:
          token: ${{ secrets.RHUB_TOKEN }}
          job-config: ${{ matrix.config.job-config }}

  other-platforms:
    needs: setup
    if: ${{ needs.setup.outputs.platforms != '[]' }}
    runs-on: ${{ matrix.config.os }}
    name: ${{ matrix.config.label }}
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJson(needs.setup.outputs.platforms) }}

    steps:
      - uses: r-hub/actions/checkout@v1
      - uses: r-hub/actions/setup-r@v1
        with:
          job-config: ${{ matrix.config.job-config }}
          token: ${{ secrets.RHUB_TOKEN }}
      - uses: r-hub/actions/platform-info@v1
        with:
          token: ${{ secrets.RHUB_TOKEN }}
          job-config: ${{ matrix.config.job-config }}
      - uses: r-hub/actions/setup-deps@v1
        with:
          job-config: ${{ matrix.config.job-config }}
          token: ${{ secrets.RHUB_TOKEN }}
      - uses: r-hub/actions/run-check@v1
        with:
          job-config: ${{ matrix.config.job-config }}
          token: ${{ secrets.RHUB_TOKEN }}
