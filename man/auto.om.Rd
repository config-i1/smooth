% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto-om.R
\name{auto.om}
\alias{auto.om}
\title{Automatic Occurrence Model Selection}
\usage{
auto.om(data, model = "ZXZ", lags = c(frequency(data)), orders = list(ar
  = c(3, 3), i = c(2, 1), ma = c(3, 3), select = TRUE), formula = NULL,
  regressors = c("use", "select", "adapt"), occurrence = c("fixed",
  "odds-ratio", "inverse-odds-ratio", "direct", "general"), h = 0,
  holdout = FALSE, persistence = NULL, phi = NULL,
  initial = c("backcasting", "optimal", "two-stage", "complete"),
  arma = NULL, ic = c("AICc", "AIC", "BIC", "BICc"), bounds = c("usual",
  "admissible", "none"), silent = TRUE, parallel = FALSE,
  ets = c("conventional", "adam"), ...)
}
\arguments{
\item{data}{Numeric vector, time series, or data frame. Non-binary input is
automatically binarised: any non-zero value becomes 1.}

\item{model}{Three-letter ETS specification (wildcards \code{"Z"}/\code{"X"}/\code{"Y"} supported).}

\item{lags}{Vector of seasonal lags. Defaults to \code{frequency(data)}.}

\item{orders}{ARIMA orders: \code{list(ar, i, ma, select=TRUE/FALSE)}.
When \code{select=TRUE}, ARIMA orders are selected automatically for each
occurrence type.}

\item{formula}{Optional formula for external regressors.}

\item{regressors}{How to handle regressors: \code{"use"}, \code{"select"}, or \code{"adapt"}.}

\item{occurrence}{Character vector of occurrence link types to try:
\code{"fixed"}, \code{"odds-ratio"}, \code{"inverse-odds-ratio"}, \code{"direct"}.}

\item{h}{Forecast horizon.}

\item{holdout}{If \code{TRUE}, a holdout sample of size \code{h} is withheld.}

\item{persistence}{Optional persistence (smoothing) parameter vector.}

\item{phi}{Optional damping parameter.}

\item{initial}{Initialisation method: \code{"backcasting"}, \code{"optimal"},
\code{"two-stage"}, or \code{"complete"}.}

\item{arma}{Optional fixed ARMA parameters.}

\item{ic}{Information criterion used for selection.}

\item{bounds}{Parameter bounds type.}

\item{silent}{If \code{TRUE}, suppresses progress messages and plots.}

\item{parallel}{If \code{TRUE} (or a core count), fit occurrence types in
parallel.  Requires \code{foreach} and \code{doMC} / \code{doParallel}.}

\item{ets}{ETS flavour passed to \link{om}: \code{"conventional"} or \code{"adam"}.}

\item{...}{Additional arguments forwarded to \link{om}.}
}
\value{
The best \code{om} object (lowest IC), with \code{$call} set to
  the \code{auto.om()} call and \code{$timeElapsed} recording wall time.
}
\description{
Fits \link{om} for each supplied \code{occurrence} type and returns the
model with the lowest information criterion.
}
\examples{
set.seed(42)
y <- rbinom(120, 1, 0.6)
\donttest{m <- auto.om(y, occurrence = c("fixed", "odds-ratio"))}

}
\seealso{
\link{om}, \link{auto.adam}
}
